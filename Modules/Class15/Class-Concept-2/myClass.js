const MyStringFunctions = require("./myStringFunctions");
const MyArrayFunctions = require("./myArrayFunctions");


/**
 * this-keyword:
 *      The keyword (this) is going to be used if want to 
 *          access ClassVariables and functions of the same class.
 * 
 */

/**
 * when we create object of a class;
 *      object gets:
 *          it's set of field-variables with default variables (except for static variables)
 *          
 */









